# password Generator

## Description
A python program that generates strong random passwords based on the user`s preferred password length.

## Features
- Generates secure random password
- User chooses the password length 
- Uses Uppercase letters
- Uses lowercase letters
- Uses numbers
- Uses special characters

## Technologies Used
- Python

## How to Run
1. open the terminal.
2. Run
   bash
python password_generator.py
